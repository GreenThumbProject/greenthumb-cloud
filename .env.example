# GreenThumb Cloud — Docker Compose environment

DB_USER=postgres
DB_PASSWORD=change-me
DB_NAME=greenthumb

JWT_SECRET=change-me-in-production

# Set to "true" only in local dev to skip JWT validation
DEV_AUTH_BYPASS=false
